---

- name: Install packages
  yum:
    name:
      - docker-ce
      - docker-ce-cli
      - device-mapper-persistent-data
      - lvm2
    state: present
  tags: docker

- name: Remove packaged docker-compose
  yum:
    name:
      -  docker-compose
    state: absent
  tags: docker

